let base = parseInt(prompt("escribe la base"));
let exponente = parseInt(prompt("escribe la exponente"));
let resultado;


const potencia = (base, exponente) => {
  this.base;
  this.exponente;
  if(exponente ==0){
    return 1;
  }else {
    resultado = Math.pow(base, exponente);
    console.log(resultado)
    return resultado;
  }
}
potencia(base, exponente);